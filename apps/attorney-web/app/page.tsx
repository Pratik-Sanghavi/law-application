"use client";
import {useEffect,useState} from "react";
export default function Page(){const [leads,setLeads]=useState<any[]>([]);const [e,setE]=useState("");async function load(){const r=await fetch("/api/leads");if(r.status===401){location.href="/api/auth/login";return}if(!r.ok){setE("Could not load leads");return}setLeads((await r.json()).items)}useEffect(()=>{load()},[]);async function mark(id:string){const r=await fetch(`/api/leads/${id}/state`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:'{"state":"REACHED_OUT"}'});if(!r.ok)setE("Update failed");else load()}return <main><h1>Lead dashboard</h1><a href="/api/auth/login">Sign in</a>{e&&<p>{e}</p>}<table><thead><tr><th>Name</th><th>Email</th><th>CV</th><th>State</th><th/></tr></thead><tbody>{leads.map(x=><tr key={x.id}><td>{x.first_name} {x.last_name}</td><td>{x.email}</td><td>{x.resume_filename}</td><td>{x.state}</td><td>{x.state==="PENDING"&&<button onClick={()=>mark(x.id)}>Mark reached out</button>}</td></tr>)}</tbody></table></main>}